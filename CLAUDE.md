# Ghar Kharcha — Claude Instructions

## Git workflow
After completing any change, always merge to `main` and push to `origin/main`.
Fast-forward merge from the feature branch, then push — no separate PR step needed unless explicitly asked.
